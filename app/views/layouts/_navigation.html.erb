<% @stories ||= [] %>
<ul>
<% @stories.each do |story| %>
<li><%= story.name %>
	<ul>
		<% story.pages.visible.sorted.each do |page|%>
		<li><%= link_to(page.name, :action => 'show', :permalink =>
		page.permalink) %>
		</li> 
		<% end%>
	</ul>
</li>
<%end%>
</ul>

<% if object && object.errors.size > 0 %>
<div id="errorExplanation">
	<h2><%= pluralize(object.errors.size , "error") %> prevent this record from being saved</h2>
	<p>There were problems with the highlighted fields;</p>
	<ul>
		<%= object.errors.full_messages do |msg| %>
		<li><%= msg %></li><% end%>
	</ul>
</div>

<%end%>